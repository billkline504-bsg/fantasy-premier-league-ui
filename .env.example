# Copy to .env.local for local development (Vite loads .env.local automatically, and it's
# gitignored). See Architecture v1.1 §14 — this base URL is the only thing that changes
# between local/staging/production builds.
VITE_API_BASE_URL=http://localhost:5000/api/v1
